/**
 * Brand constants.
 *
 * "Jovoc" is an acronym, and its expansion is deliberately the same in every
 * locale — it is the name of the thing, not a phrase to be translated. The
 * Bengali pages carry it in Latin script exactly as the English pages do.
 */

export const BRAND_NAME = 'Jovoc';

export const BRAND_EXPANSION = 'Journey of Vocation & Contentment';

/** Brand plus its expansion, for titles and headers. */
export const BRAND_FULL = `${BRAND_NAME} — ${BRAND_EXPANSION}`;
